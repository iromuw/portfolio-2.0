import type { RawFileNode } from '../../types'
import { BIO_SNIPPET } from '../../snippets'

export const experience: RawFileNode = {
  type: 'file',
  iconColor: 'bg-amber-400',
  content: `/**
 * Work Experience
 *
 * placeholder experience...
 */`,
  snippets: [BIO_SNIPPET],
}
